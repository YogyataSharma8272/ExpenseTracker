# Backend Configuration (.env)

# MongoDB Atlas URI - REQUIRED for deployment
MONGODB_URI=mongodb+srv://yogyatasharma1515_db_user:KV3eeazrx3IRPCY7@cluster0.6pfnn5o.mongodb.net/expensetracker?retryWrites=true&w=majority&tls=true

# JWT Secret for token signing - REQUIRED for authentication
JWT_SECRET=your-strong-secret-key-change-this-in-production

# Port for backend server (optional - defaults to 5000)
PORT=5000

# Environment mode
NODE_ENV=development

# For Vercel Deployment:
# Go to: Vercel Dashboard → Project → Settings → Environment Variables
# Add these keys with Production scope:
# - MONGODB_URI
# - JWT_SECRET
# Then redeploy or create new commit to trigger auto-deployment
